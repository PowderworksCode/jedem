# @generated by jedem for the `python` target. Do not edit.
# Surface `hello` v0.1.0. Regenerate with `cargo jedem generate`.

[package]
name = "hello-python"
description = "The Python binding for `hello`, generated by jedem."
version = "0.1.0"
edition = "2021"
publish = false

[lib]
# Must match the `#[pymodule]` function, and the file Python imports.
name = "hello"
crate-type = ["cdylib"]

[dependencies]
hello = { path = "../.." }
pyo3 = { version = "0.23", features = ["extension-module"] }
