[package]
name = "jedem"
description = "Expose a Rust function once; call it from every language, with its shape intact."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
jedem-macros = { workspace = true }
