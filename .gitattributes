# Files jedem writes. Collapsed in review by default; regenerate rather than
# editing. Every one also carries an `@generated` marker in its own header.
demo/python/** linguist-generated=true
demo/node/**   linguist-generated=true
